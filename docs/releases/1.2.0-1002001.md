# StarRustDesk 1.2.0（1002001）

## 新增与优化

- 手机和平板新增可开关的虚拟鼠标，支持左右键点击、按住拖动、滚轮滚动和中键操作，并记住开关状态。
- 虚拟鼠标采用透明蓝灰玻璃外观、细描边和淡蓝色按下反馈，清晰区分左右键、滚轮与移动区域。
- 修复虚拟鼠标与远端光标漂移、缩放和平移后位置不同步的问题，改善多屏切换后的光标对齐。
- 修复关闭虚拟鼠标后按钮仍显示选中，以及左右键、滚轮点击无效或重复触发的问题。
- 开启虚拟鼠标并放大画面后，可拖动鼠标移动远端光标，单指拖动画面其他区域平移视野。
- 手机和平板横屏时，“键”默认位于左上角、“控”位于右上角，展开方向改为向下；两组悬浮栏支持在屏幕内自由拖动。
- 长按“键 / 控”栏中的具体按钮可自定义排列顺序并保存；优化横屏软键盘弹出后的画面避让。

应用内更新弹窗和设置中的更新日志同步更新，最新内容位于最上方；更新后首次启动自动显示。

## 下载说明

- 本 Release 仅提供 Release 模式构建的未签名 HAP，需要自行签名后安装。
- 用于华为应用市场的正式签名 APP 仅保存在本地，不上传 GitHub。

## 构建验证

- 16 组 JavaScript 回归检查及 33 项 Rust 核心测试通过。
- Release 构建通过；包内版本为 `1.2.0（1002001）`，`debug=false`，包含 ARM64 和 x86_64 原生库。
- 正式 APP 外层签名、内层 HAP 签名、摘要及 Release Profile 验证通过；发布类型为 `app_gallery`，内层 HAP 字节一致。
- 正式 APP SHA-256：`07712FAD4660565E349AE2B056E788309B31EAE8A7C977140490EEB0CE92AABD`
- 未签名 HAP SHA-256：`EEC85204230F25219F6CA420C056F8285FC0272FAF9073D263BF2C0199E7B87C`
