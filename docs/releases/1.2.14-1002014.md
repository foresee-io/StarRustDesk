# StarRustDesk 1.2.14（1002014）

## 更新内容

1. 虚拟鼠标拖到远端画面的右侧或下侧边缘后，继续拖动可移开画面，露出黑色操作空间；控件和自带箭头保持贴合。
2. 修复从画面其他区域平移后虚拟鼠标控件与箭头错位，反向拖动可收回边缘空白。
3. 开启虚拟鼠标时，控件外的单指拖动只用于移动画面，不再误移动远端光标。

## 下载与验证

GitHub Release 仅提供**未签名 HAP**，须自行签名后安装。正式签名的 AppGallery `.app` 仅保存在本地用于上架，不上传 GitHub。

- 包名：`com.yinhe.starrustdesk`；版本：`1.2.14（1002014）`。
- AppGallery 正式签名 `.app` SHA-256：`3A1E63A22E885496C3B0BBBC65AF4B127E298C7807F452F3CA63FDEE20B451FD`；已验证外层和内层 HAP 签名、Release 证书配置与包元数据。
- GitHub Release 未签名 `.hap` SHA-256：`B07F6C1C094ABEE6E6A4F75853067471F0A2F23A2133223507F2575344F6D69B`。
- 实机已验证右侧和下侧边缘黑边操作；最终补充的“其他区域不再误移动光标”已通过自动测试与构建，但尚未完成重装后的实机复核。

自动测试、构建和签名验证不等于应用市场审核通过。
